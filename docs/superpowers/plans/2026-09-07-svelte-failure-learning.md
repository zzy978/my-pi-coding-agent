# Svelte 真实失败经验执行计划

> 在当前会话按 executing-plans 执行；用户已授权准备、真实模型基线、合格失败分析和相关任务配对实验。晋升另需人工明确批准。

**目标：** 固定五道 Svelte 历史缺陷，先验证环境，再收集三道来源题的真实模型结果，最后检验候选迁移收益。

**架构：** 复用现有受控运行、经验与实验模块。独立基准适配器负责 Git 快照、无网络 Linux 容器、Mocha 结果与测试完整性。生产源码不在本轮修改范围。

**技术：** Node.js ESM、Docker Linux、Git、Mocha、Pi ControlledPiRuntime。

**依据：** 本会话用户批准的三阶段顺序。

## 约束

- 独立源仓库 D:/svelte-real-v1；基准目录 .picoding/benchmarks/svelte-real-v1；数据根 .picoding/svelte-real-v1。
- 来源题 6414、4146、5850；留出题 4558、7422 只做确定性红绿准备，不提前运行模型。
- 复用 deepseek/deepseek-v4-flash、high、策略 3 与七项现有工具；不通过限制预算制造失败。
- 固定镜像 ID、基线、测试补丁、命令与判定器哈希；模型不能修改测试及配置来获取通过。
- 基线测试须有指定 F2P 失败且 P2P 通过；参考修复须全部指定测试通过。基础设施失败不进入经验提炼。
- 参考修复只供准备阶段使用，不进入模型任务上下文；宿主跨目录工具不是安全隔离，另记录证据边界。
- 每项正式配对至少 3 对，新鲜两臂；不自动晋升，不混用历史失败作对照。

## 执行步骤

- [x] 1. 建立 adapter.test.mjs，覆盖 Mocha 红/绿、遗漏测试、重复名称、跳过及额外失败；先运行失败，再实现 adapter.mjs。
- [x] 2. prepare.mjs 拉取五个固定镜像，核对上游提交，运行红绿测试，将 Git 无父历史快照及 TaskSpec 写入 catalog.json/tasks。
- [x] 3. verify.mjs 核查源码基线与判定器哈希，拒绝测试/配置改动，以固定容器执行验证；验证实际 Windows 快照空补丁为红。
- [x] 4. run.mjs 按 6414、4146、5850 执行并保留脱敏工具诊断、源码补丁、费用与状态；任一基础设施异常先修环境再继续。
- [x] 5. 审查失败 diff、验证输出和工具错误。只对证据支持的真实编码失败调用现有 --analyze-run。
- [x] 6. 审查候选适用性，选择非来源题。为目标题记录配置后执行 3 对；记录留出题开启时间与用途，不强行迁移无关候选。原 15 分钟实验未完成，用户授权的独立 45 分钟实验已完成。
- [x] 7. 汇总红绿证据、基线和实验结果；运行脚本测试、语法核对及 git diff --check，说明未验证项。

## 已确认的证据与调整

- 五题全部本地红绿验收完成；6414、4146、5850 三次真实基线均已记录。
- 6414 和 5850 原运行都因 25 分钟预算中止，原始结果不改。最终补丁独立补验分别失败、通过，因此只有 6414 有确定的编码失败证据。
- 默认经验分析误归因于工具错误。基准适配器补入完整补丁、补验和诊断后重新合成；其结论仍需人工审查，不表示生产分析器已改好。
- 4558、7422 尚未调用模型。根据 slot/transition 数据流相关性补入 6042，并在模型运行前选定候选 4525f627-b572-4ffc-bd4d-9a9ed67fa0ad。
- 6042 本地重建环境已完成红绿验收，共 3340 个测试实例，2 个 F2P。公共镜像拉取被拒的记录保留。
- 配对使用现有实验服务的每臂 15 分钟预算，与原基线 25 分钟分开报告；模型运行超时若导致实验停止，必须报告不完整，不伪造三对完成。

## 最终执行状态

第 6 步已发起，未完成三对。实验 36b8bdf3-f918-4dd5-9453-6190bb9d630a 在第一个对照臂达到默认 15 分钟模型预算后自动停止，完整配对 0/3，候选臂未调用，结论 inconclusive。不以历史基线补足，也不在观察结果后临时修改预算继续此项实验。

最终复核：5 个 run bundle、2 个经验、1 个实验通过生产加载校验；记录在 manifest 中的验证器哈希与当前文件/环境相符；4558、7422 未调用模型；受管 worktree 和本轮测试容器清理完成。判定脚本 6 项测试通过，git diff --check 通过；生产源码未改。

SDK 费用估计合计约 0.314231 美元。真实编码失败样本已经获得，经验收益尚未验证。完整本地报告：D:/Agent/.picoding/benchmarks/svelte-real-v1/report.md。

## 用户授权的 45 分钟重跑

用户明确要求对照臂和候选臂均为 45 分钟，重新执行同一任务。增加实验 API 的统一预算和持久字段，保持旧记录兼容；本次运行 experiment-45m.mjs，复用 6042 冻结任务及原候选，重新执行 3 对，不以旧运行补足。先完成类型检查、完整测试、lint、build 及历史证据加载，再调用真实模型。新结果单独保存到 transfer-result-45m.json。

重跑已完成：实验 b958009f-7924-460e-bd1e-088043ee5bcc，完整配对 3/3，六臂 manifest 均为 2700000 毫秒；对照与候选均 3/3 通过，结论 no_observed_gain。平均整次运行时间分别 32.31、19.67 分钟；本轮 SDK 费用估计 0.397180 美元。时间与费用差异只作描述性观察，不证明稳定收益，不晋升候选。

生产代码检查、288 项测试、lint、build 均通过。最终 11 个运行、2 个经验、2 个实验通过加载校验，冻结验证器哈希一致，4558、7422 仍未调用模型，受管工作树和测试容器清理完成。对照臂 1 的目录外临时复现文件、对照臂 3 的目录外日志均已归档核对并逐个清理；后继候选臂轨迹未发现读取，但短暂可访问窗口及非沙箱限制必须保留。中间编译错误与环境错误的适配器误标另行记录。完整报告：D:/Agent/.picoding/benchmarks/svelte-real-v1/report-45m.md。
